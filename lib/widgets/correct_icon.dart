import 'package:flutter/material.dart';

class CorrectIcon extends StatelessWidget {
  const CorrectIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.check,
      color: Colors.green,
    );
  }
}
